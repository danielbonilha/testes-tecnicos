package iniciante.loops.b;

import java.util.ArrayList;
import java.util.List;

public class DecreasingLoop {
    public List<Integer> reverseItems(List<Integer> items) {
        List<Integer> reversedItems = new ArrayList<>();
        // TODO
        return reversedItems;
    }
}

/*
# Problema:

Retorne uma nova lista de inteiros na ordem inversa da lista 'items'

# Dicas:

Será preciso realizar um loop decrescente sobre a lista, utilizando a notação
for (int i = items.size() - 1; i >= 0; i--) { ... }

Para adicionar um item na nova lista use 'reversedItems.add(...)'

# Exemplos
Items [0, 5, 10, 15, 20] retorna [20, 15, 10, 5, 0].
 */